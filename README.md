# spike-Shell

Simple shell emulator on java.
Supports commands:
- echo
- exit
- ls (with -l and -a keys only)
- cd

##Goals:
- play with java IO/NIO API.

##Not goals:
- Stability
- Tests
- Usefulness